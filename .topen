#!/bin/bash

# It seems to only work from here :(
if [ -f $HOME/.Xmodmap ]; then
    /usr/bin/xmodmap $HOME/.Xmodmap
fi

dpkg --get-selections | grep 'install' | sed 's/^\(\w\+\).*/\1/g' | sort | unig > $HOME/.installed_packages

#tmux a || tmux new
